package org.proygrad.einstein.service.nontransactional;

//@Component
public class CalculationJob {
/*
   @Autowired
    private ScenarioDAO scenarioDAO;

    @Autowired
    private CalculationService calculationService;

   //  @Scheduled(fixedDelay = )
    private void executeJob(){
        // TODO: Levantar de la BD las REQUEST (topear) y solicitar su resolucion.

        List<ScenarioEntity> request = scenarioDAO.getPending();

        for(ScenarioEntity entity : request)
            calculationService.calculationResolve(entity);
        }

    }

*/

}
